import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { ArrowLeft, BookOpen, HelpCircle } from 'lucide-react';
import { useLocation, Link } from 'wouter';

export default function About() {
  const [_, setLocation] = useLocation();

  return (
    <main className="flex flex-col h-[100dvh] w-full bg-black text-foreground overflow-auto">
      <header className="absolute top-0 inset-x-0 z-30 flex items-center justify-start p-4">
        <Button variant="ghost" size="icon" onClick={() => setLocation('/')}>
          <ArrowLeft className="size-5" />
        </Button>
      </header>

      <div className="container mx-auto max-w-3xl py-20 px-4">
        <h1 className="font-serif text-4xl md:text-5xl text-center text-white/90 tracking-widest uppercase mb-12">
          About The 25th Chapter
        </h1>

        <div className="space-y-8">
          <Card className="bg-white/5 border-white/10 text-white/80">
            <CardHeader>
              <CardTitle className="font-mono text-lg tracking-wider">What is this?</CardTitle>
            </CardHeader>
            <CardContent className="space-y-4 font-mono text-sm">
              <p>
                The 25th Chapter is a real-time, interactive, AI-driven fictional story platform where readers vote on choices to progress the story. 
                Content is generated by the Google Gemini API (both text and 16:9 accompanying images).
              </p>
            </CardContent>
          </Card>
          
          <Card className="bg-white/5 border-white/10 text-white/80">
            <CardHeader>
              <CardTitle className="font-mono text-lg tracking-wider">Learn More</CardTitle>
            </CardHeader>
            <CardContent className="space-y-4 font-mono text-sm">
              <Link to="/install">
                <Button variant="outline" className="mt-2 mr-2">
                  <BookOpen className="size-4 mr-2" />
                  Installation Guide
                </Button>
              </Link>
              <Link href="/upcoming#faq">
                <Button variant="outline" className="mt-2">
                  <HelpCircle className="size-4 mr-2" />
                  FAQ
                </Button>
              </Link>
            </CardContent>
          </Card>
        </div>
      </div>
    </main>
  );
}
