This document serves as the master specification and architectural blueprint for the Narrative Ledger Protocol (NLP).You are instructed to implement the NarrativeEngine module and integrate it into the 25th Chapter repository. This requires a systematic refactor of the existing RAG pipeline to achieve a deterministic, highly observable narrative state.Here is the exact execution plan. Do not deviate.1. Database Schema & Infrastructure (schema.ts)The foundation of the NLP requires structured state persistence.Vector Extension: Ensure pgvector is enabled in the PostgreSQL instance.Update blocks Table:Add isNotable: boolean("is_notable").default(false).notNull()Add embedding: vector("embedding", { dimensions: 768 }) (adjust dimensions to match the embedding model used, e.g., 768 for Google's standard embedding API).Create lore Table:id: serial("id").primaryKey()channelId: text("channel_id").notNull()content: text("content").notNull() (Semantic sentences)isActive: boolean("is_active").default(true).notNull()createdAt: timestamp("created_at").defaultNow()Zero-Deletion Policy: Under no circumstances are you to generate SQL or ORM operations containing a DELETE statement with a WHERE clause. Deprecated records must use soft-deletes (e.g., setting isActive to false).2. The Provider Interface (src/engine/provider.ts)Implement Dependency Inversion to decouple the engine from Drizzle ORM.Define the NarrativeProvider interface:getLoreAtoms(channelId: string): Promise<Lore[]>getNotableEvents(channelId: string): Promise<Block[]>getBlocksByIndices(channelId: string, indices: number[]): Promise<Block[]>getHybridSearchCandidates(channelId: string, query: string, limit: number): Promise<Array<Block & { scoreVectorDense: number, scoreKeywordSparse: number }>>getBlockCount(channelId: string): Promise<number>3. Drizzle Storage Implementation (src/storage.ts)Implement the NarrativeProvider methods within DatabaseStorage.Hybrid Search Query (getHybridSearchCandidates): Write a raw SQL/Drizzle query that computes both dense and sparse scores.Vector Score: Calculate cosine similarity as 1 - (blocks.embedding <=> query_embedding).Keyword Score: Calculate ts_rank(to_tsvector('english', blocks.content), plainto_tsquery('english', query)).Normalize the Keyword Score dynamically by dividing each row's ts_rank by the MAX(ts_rank) OVER() in the current result set to ensure a $0.0 \to 1.0$ boundary.Limit the fetch to the top 20 candidates.4. The NarrativeEngine Module (src/engine/index.ts)This is a standalone class initialized with constructor(private provider: NarrativeProvider). It contains the Context Orchestrator logic.Method: generateContext(channelId: string, inputQuery: string): Promise<string>Implement the execution waterfall exactly as follows:Harvest Phase: * Fetch active Lore Atoms.Calculate the Reciprocal Skeleton indices (using the existing generateReciprocalSequence logic from rag.ts). Fetch these blocks.Fetch Hybrid Search candidates using inputQuery.Scoring & Fusion Phase:Iterate through the Hybrid candidates. Apply the Fused Relevance Formula:$$Score = (scoreVectorDense \times 0.7) + (scoreKeywordSparse \times 0.3)$$Apply the Significance Coefficient: If isNotable === true, multiply the $Score$ by $1.5$.Saliency Gate Phase:Filter out any candidate whose final score is < 0.65.Sort the survivors by score (descending) and take the Top 3.Timeline Alignment Phase:Merge the Reciprocal blocks and the Top 3 Hybrid survivors. Deduplicate by id.Sort this final array chronologically by id (ascending).Map each block to its temporal offset: const offsetHistorical = currentBlockCount - blockIndex.Prose Injection Phase:Compose the final prompt string using Natural Prose. Do NOT use markdown headers or JSON arrays.Format: "To maintain consistency with the established story: [Lore Atoms joined by spaces]. [Offset] storyblocks ago, [Block Content]. [Offset] storyblocks ago, [Block Content]..."Append the immediateContext at the end.5. Trace Observability (src/engine/trace.ts)Implement a strict, verbose debugging logger.Create a loggerNarrativeTrace(traceObject: any) function.If process.env.NODE_ENV === 'development', write the complete traceObject (Input, Lore used, all Hybrid candidates with raw scores, Survivors, and Final Prompt) to the filesystem (e.g., .traces/trace_${timestamp}.json).Ensure tight try/catch error handling. If logging fails, it must not crash the narrative generation.6. Refactor Existing Logic (src/ai.ts & src/rag.ts)Root Cause Analysis of Legacy Code: The previous buildRAGContext logic suffered from "Contextual Lottery"—it blindly selected indices, entirely missing highly relevant plot points, resulting in severe contextual drift.Refactor: Deprecate the existing buildRAGContext. Instantiate NarrativeEngine in ai.ts.Replace the RAG call in generateStoryBlock to use engine.generateContext(channelId, previousContext).Implement a circuit breaker: If generateContext times out (e.g., > 3000ms) or throws an uncaught error, gracefully catch it, log the error trace, and fallback to returning only the previousContext.7. Vitest Test Suite (src/engine/engine.test.ts)Write comprehensive tests using Vitest.Mock the NarrativeProvider.Assert that isNotable blocks with low base scores defeat standard blocks with high base scores (verifying the $1.5 \times$ coefficient).Assert that blocks scoring $< 0.65$ are stripped from the output.Assert chronological sorting (Ascending IDs).Assert the fallback circuit breaker returns the immediate context on provider failure.Execute this implementation in a single, comprehensive pass. Ensure all variable names follow a least-to-most-descriptive convention (e.g., scoreFinalFused, blocksHistoricalReciprocal).