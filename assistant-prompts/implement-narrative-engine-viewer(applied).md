MASTER DIRECTIVE: Implement the Narrative Ledger Protocol & Narrative ViewerYou are to implement the NarrativeEngine as a standalone, modular package and its integrated observability suite, the Narrative Viewer. You must execute this in a single pass with 100% production readiness.1. Module Architecture & CLI (packages/narrative-engine)Package Identity: Initialize packages/narrative-engine.The CLI Entry: Configure the bin field in package.json to map narrative-viewer to a local script.Command: npx narrative-viewer (or npm run viewer) must launch the React-based Narrative Viewer.The Attachment: Export a attachNarrativeViewer(app: Express) function. This function must register the Shadow Routes required for the Viewer to pull live TraceObjects and execute "Lab Generations."2. The Narrative Viewer (React UI)The Viewer is a high-fidelity "Laboratory" for story orchestration. It must be accessible via a local web server (e.g., port 5173).UI Requirement: The "Synchronized Diff" DashboardLore Atom Highlighter: A panel showing all LoreAtoms. Use high-contrast indicators to show which atoms were Selected vs. Pruned for the current prompt.Trace Inspector: A view of the "finalized" prompt string exactly as it was composed before being sent to the LLM.Live Parameter Lab: Implement sliders and toggles that allow real-time "Re-Scoring" of the current trace:Saliency Slider: Adjust the 0.65 threshold (The Noise Gate).Weight Toggle: Shift the 0.7/0.3 Fused Relevance balance.Significance Coefficient: Adjust the 1.5x multiplier for Narrative Anchors.Temporal Phrasing Switch: Toggle between "Storyblocks ago" and raw IDs.Lab Environment: A "Test Generate" button that triggers a story block generation using the current UI-adjusted parameters, but intercepts the database write so it does not affect production readers.3. Execution Flow: The "Waterfall"The NarrativeEngine.generateContext() method must follow this strict sequence:The Harvest: Concurrently fetch 20 Vector candidates and 20 Keyword candidates.The Fusion: Apply $Score = (Vector \times 0.7) + (Keyword \times 0.3)$.The Anchor Boost: Apply $1.5x$ multiplier to isNotable blocks.The Saliency Gate: Evict any block scoring below 0.65.Temporal Alignment: Re-sort the Top 3 survivors + Reciprocal Skeleton chronologically.Natural Prose Recap: Format as: "To maintain consistency with the established story: [Lore]. [X] storyblocks ago, [Content]..."4. Trace Pipeline & Filesystem LoggingAutomated Audit: Every generation in development mode must append a TraceObject to .traces/narrative_ledger.jsonl.Visibility: This log must include the "Finalized" prompt string and the raw scores of all discarded candidates to power the Viewer’s slider logic.5. Refactoring & Integration (25th Chapter)Refactor src/ai.ts: Remove legacy buildRAGContext. Replace with a call to the NarrativeEngine.Storage Adapter: Implement the NarrativeProvider interface in the main application using Drizzle ORM to supply the engine with blocks and lore.The Circuit Breaker: If the NarrativeEngine exceeds a 3000ms timeout or throws an error, catch it and return only the previousContext to ensure the user session remains alive.6. Principal Engineering StandardsZero-Deletion Policy: Do not use DELETE with WHERE. Use soft-deletes only.Vitest: Implement 100% coverage for the engine's scoring logic, temporal sorting, and saliency filtering.Idiomatic Lexicon: Ensure all code comments and variable names use the established lexicon (e.g., saliencyGateThreshold, significanceCoefficient).